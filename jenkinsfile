pipeline {
    agent any
    stages {
        stage('Test Artifactory Connection') {
            steps {
                script {
                    def server = Artifactory.server('my-jfrog-artifactory')
                    echo "JFrog Server ID loaded: ${server}"
                }
            }
        }
    }
}
